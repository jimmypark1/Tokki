package com.Whowant.Tokki.Utils;

public interface OnKeyboardVisibilityListener {                                                         // 키보드 보여지고 사라질때를 위한 리스너 interface
    void onVisibilityChanged(boolean visible);
}
